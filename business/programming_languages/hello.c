#include <stdio.h>

// C is a compiled language, we need a main function and we use printf to print to the console.
// You can compile this code using a C compiler and run the resulting executable.
// 'clang -o hello hello.c' will compile the code into an executable named 'hello', run it with './hello' and you will see the output "hello, world" in the console.
int main(void)
{
    printf("hello, world\n");
}